const initialState = {
    episodes: [],
    isLoading: false,
    currentPage: 1

}

export const EpisodesReducer = (state = initialState, action) => {
    switch (action.type) {


        default:
            return state
    }
}